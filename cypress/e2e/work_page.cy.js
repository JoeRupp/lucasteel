describe("LucaSteel work page", () => {
  beforeEach(() => {
    cy.visit("http://localhost:3000").wait(1000);
  });

  it("should include a list of projects", () => {});

  it("should open up a more detailed individual project page when a project is clicked", () => {});

  it("should have a project page that includes a hero image, title, info, and pictures", () => {});

  it("should project pictures that open up a modal of an enlarged image when click", () => {});
});
